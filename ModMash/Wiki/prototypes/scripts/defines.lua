if not wiki then wiki = {} end
if not wiki.defines then wiki.defines = {} end
if not wiki.defines.data_stages then wiki.defines.data_stages = {} end
 
wiki.defines.data_stages.data = "data"
wiki.defines.data_stages.data_final_fixes = "data-final-fixes"
wiki.defines.data_stages.data_updates = "data-updates"
wiki.defines.data_stages.control = "control"